<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.kr.skt.haruman" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name=".MyHarumanApplication"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".AlbaListActivity"
            android:label="하루만"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SubstituteActivity" android:label="하루만"/>
        <activity android:name=".SettingActivity" android:label="설정"/>
        <activity android:name=".AfterActivity" android:label="만족도"/>
        <activity android:name=".AfterInsertActivity" android:label="만족도 글쓰기"/>
    </application>

</manifest>
